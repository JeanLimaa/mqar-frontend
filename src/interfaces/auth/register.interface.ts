export interface FormValues {
    firstName: string;
    email: string;
    password: string;
}

export interface Errors {
    firstName: string;
    email: string;
    password: string;
    generalistError: string;
}